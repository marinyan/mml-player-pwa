# Tuplets

`{ C D E }4` fits three notes into one quarter note. Notes and rests inside the
braces are distributed evenly; individual length suffixes are ignored.

`{ C D E }4.` uses a dotted quarter note as the total duration.

Tuplets cannot be nested or contain tempo, time-signature, measure-boundary, or
track-separator commands.
